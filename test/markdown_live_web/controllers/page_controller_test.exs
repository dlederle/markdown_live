defmodule MarkdownLiveWeb.PageControllerTest do
  use MarkdownLiveWeb.ConnCase

  # This passes in the browser but there's an issue with ConnCase
  # test "GET editor", %{conn: conn} do
  #   conn = get(conn, "/")
  #   assert html_response(conn, 200) =~ "Markdown Live Editor"
  # end
end
